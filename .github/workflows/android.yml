name: Android CI

on:
  pull_request:
    branches:
      - develop
  push:
    branches:
      - 'test/**'
      - main
      - master

jobs:

  # ── Runs on every trigger: PR to develop, push to test/**, main, master ──
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Create github.properties
        run: touch github.properties

      - name: Run unit tests
        run: ./gradlew test

      - name: Upload test report
        uses: actions/upload-artifact@v6
        with:
          name: TestResults
          path: build/reports/tests/testDebugUnitTest/

      - name: Generate coverage report
        run: ./gradlew jacocoTestReport

      - name: Upload coverage report
        uses: actions/upload-artifact@v6
        with:
          name: CoverageReport
          path: build/reports/jacoco/jacocoTestReport/html/

      - name: Run lint
        run: ./gradlew lint

      - name: Upload lint report
        uses: actions/upload-artifact@v6
        with:
          name: LintResult
          path: build/reports/lint-results*.html

  # ── Only on push to test/** — builds debug and release AARs for QA ──
  build_test_candidate:
    needs: test
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/heads/test/')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Create github.properties
        run: touch github.properties

      - name: Get branch name
        id: extract_branch
        run: echo "branch=$(echo ${GITHUB_REF##*/})" >> $GITHUB_OUTPUT

      - name: Build AARs
        run: ./gradlew -PversName=${{ steps.extract_branch.outputs.branch }} clean build

      - name: Set AAR names
        run: |
          echo "DEBUG_AAR=$(basename $(ls build/outputs/aar/*-debug.aar))" >> $GITHUB_ENV
          echo "RELEASE_AAR=$(basename $(ls build/outputs/aar/*-release.aar))" >> $GITHUB_ENV

      - name: Upload debug AAR
        uses: actions/upload-artifact@v6
        with:
          name: ${{ env.DEBUG_AAR }}
          path: build/outputs/aar/${{ env.DEBUG_AAR }}

      - name: Upload release AAR
        uses: actions/upload-artifact@v6
        with:
          name: ${{ env.RELEASE_AAR }}
          path: build/outputs/aar/${{ env.RELEASE_AAR }}

  # ── Only on push to main/master — creates a GitHub release and publishes ──
  publish_release:
    needs: test
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Create github.properties
        run: touch github.properties

      - name: Retrieve version name
        id: app_version
        run: echo "VERSION_NAME=$(./gradlew -q versionName)" >> $GITHUB_OUTPUT

      - name: Build AAR
        run: ./gradlew build

      - name: Set app name
        run: echo "APP_NAME=$(basename $(ls build/outputs/aar/*release.aar | head -n 1))" >> $GITHUB_ENV

      - name: Create GitHub release and upload asset
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.app_version.outputs.VERSION_NAME }}
          name: Release ${{ steps.app_version.outputs.VERSION_NAME }}
          files: build/outputs/aar/${{ env.APP_NAME }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish to GitHub Packages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./gradlew publish
