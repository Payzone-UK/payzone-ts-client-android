name: Pull Request

on:
  pull_request:
    branches:
      - develop

jobs:
  run_test:
    uses: ./.github/workflows/android-build.yml
    with:
      build_type: debug
      upload_artifacts: false
